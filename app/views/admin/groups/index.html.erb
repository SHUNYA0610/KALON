<div class="row">

    <%= render 'layouts/sidebar' %>

  <div style="flex:4;">
    <div>
      <h2>グループ一覧</h2>
      <table class="table table-hover table-inverse">
        <thead>
          <tr>
            <th></th>
            <th>グループ名</th>
            <th>紹介文</th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <% @groups.each do |group| %>
            <tr>
              <% if group.group_image.attached? %>
              <td class="align-middle"><%= image_tag group.group_image.variant(resize_to_fill: [50, 50]).processed,class: "rounded-circle" %></td>
              <% else %>
              <td class="align-middle"><%= image_tag "no-image.jpg", size: "50x50",class: "rounded-circle" %></td>
              <% end %>
              <td class="align-middle"><%= link_to group.name, admin_group_path(group) %></td>
              <td class="align-middle"><%= group.introduction %></td>
              <td class="align-middle"><%= link_to '削除', admin_group_path(group), method: :delete, data: { confirm: '本当に削除しますか？' }, class: "btn btn-sm btn-outline-danger" %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
  
    <%= render "layouts/sidebar_r" %>

</div>